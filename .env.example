# Copy to .env and set the address the phone can reach.
# Same Wi-Fi: your computer LAN IP (run ipconfig to find it).
# Different location: a public tunnel to the backend, e.g. ngrok http 4000
EXPO_PUBLIC_API_URL=http://192.168.1.100:4000
